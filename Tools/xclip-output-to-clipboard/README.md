# Copy to clipboard using xclip

Called by `copy-to-clipboard` (in aliases.sh) or `xclip-output-to-clipboard.sh` (for desciription purposes)

---

## The BEST script I ever (maybe not) wrote

Copy all whole output of a command to clipboard using `xclip` and paste that in a AI chat, thanks me later